import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  option: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 7,
  },

  radioBtn: {
    width: 20,
    height: 20,
    borderRadius: 100,
    borderWidth: 2,
    marginRight: 1,
  },
});
